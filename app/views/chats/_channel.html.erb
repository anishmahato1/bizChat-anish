<%= link_to chat_path(channel.chat) do %>
<div id="<%= dom_id channel %>" class="channels-each">
    <div class="channel-logo"><%= channel.name[0] %></div>
    <div class="channel-name"><%= channel.name %></div>  
</div>
<% end %>